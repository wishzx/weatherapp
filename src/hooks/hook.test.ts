import { describe, expect, it } from 'vitest';

describe('the forecast weather hook', () => {
  it.todo('should convert the date from UTC to the correct timezone and day of the week', () => {
    expect.hasAssertions();
  });
});

describe('the geolocation hook', () => {
  it.todo('should be supported on Chrome, Firefox, Edge and Safari', () => {
    expect.hasAssertions();
  });
});
