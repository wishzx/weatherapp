import { describe, expect, it } from 'vitest';

describe('test my assumptiopns API', () => {
  it.todo('should not throw error API limit if called a lot of times with the same parameters', () => {
    expect.hasAssertions();
  });

  it.todo(
    'should return cached result, so 2 at most different result when calling with the same parameters many times',
    () => {
      expect.hasAssertions();
    },
  );
});

describe('test current weather endpoint', () => {
  it.todo('should fit in ts interface when called with random positions', () => {
    expect.hasAssertions();
  });
});

describe('test forecast weather endpoint', () => {
  it.todo('should fit in ts interface when called with random positions', () => {
    expect.hasAssertions();
  });
});
